// How many of the previous tier must be bought to discover a new tier
export const NB_TIERS_TO_DISCOVER = 5;
// Cost factor between each production tier unit
export const PRODUCTION_COST_SCALING_FACTOR = 1.2;
// Cost factor between each upgrade tier level
export const UPGRADE_COST_SCALING_FACTOR = 3;
// How many units of the affected tier must be owned to buy the next upgrade level
export const REQUIRED_OWNED_UPGRADES = [10, 50, 100, 150, 200, 300, 400, 500, 700, 800];
